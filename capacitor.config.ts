import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alwali.medical',
  appName: 'Al-Wali Medical System',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
